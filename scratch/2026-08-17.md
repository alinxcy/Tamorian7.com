## 2026-08-17

- 00:30 Codex(Windows ノート PC)の `258ccf3` を fetch。差分は7ファイル。
  package-lock.json も STATE/DECISIONS/CLAUDE も public/ も触られていない。CRLF も混入なし
- 00:35 `npm run check` → error 0 / warn 0。`npm run build` 通る。
  `dist/seeds/<slug>/` は出て `dist/seeds/index.html` は無い。仕様どおり
- 00:38 `npm test` が4件失敗。**08-16 に自分が schema を v2 に上げたとき
  `scripts/state.test.mjs` を直し忘れていた。** fixture が `progress: 0.5` を
  持ったままで、v2 では未知のキーなので「正しい STATE.md」が error を出す
- 00:38 Codex はこれを踏んだが直さず、`scratch/2026-08-16.md` に理由を書いて残していた。
  プロンプトで「STATE 関連は触るな」と書いた通りに動いている。正しい振る舞い
- 00:40 **なぜ気づかなかったか。** `deploy.yml` は `npm run check` と `npm run build`
  しか回さない。`npm test` は CI に載っていないので、赤くなっても誰も止まらない。
  テストがあるのに走らせていない = 無いのと同じ、という状態だった
- 00:45 fixture を v2 に更新。ついでに v1 の名残を落とすテストを足した
  (`v1 の progress が残っていたら error`)。26/26 PASS
- 00:48 `npm run check` の warn は 1 だと思い込んで Codex のプロンプトに書いていたが、
  実際は 0 だった。`879c934` で reason コメントを付けて潰してあった。自分の記憶が古い

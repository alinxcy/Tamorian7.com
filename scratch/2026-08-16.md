## 2026-08-16

- 00:09 fresh clone 後に `core.autocrlf false` を設定したら main の2ファイルが変更扱いになり、指定ブランチへ checkout できなかった。clone 時点では Git for Windows の既定設定で CRLF に変換済みだったのが原因。新規 clone 由来の2ファイルを HEAD の LF 内容へ戻して解消した。
- 00:09 portable Node 22 の `npm.cmd` を絶対パスで起動したが、最初の `npm ci` が esbuild の install script で失敗した。npm は起動できても子プロセスのPATHには `node.exe` の場所が入っておらず、`node install.js` を実行できなかったのが原因。Node のディレクトリをそのプロセスのPATHへ加えて再実行し、28.62秒で完了した。
- 00:12 `npm test` で STATE パーサーの4件が失敗した。`scripts/state.mjs` は schema v2 へ更新済みなのに、`scripts/state.test.mjs` の正常fixtureと期待値は schema v1 の `progress` 前提のままなのが原因。今回の seeds 実装とは別領域で、STATE関連の運用を勝手に直さないため変更せず残した。

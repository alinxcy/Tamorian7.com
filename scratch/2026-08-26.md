## 2026-08-26

**週のリミットが余ることが確定した日。** 消化のために、普段は重くて避けている
「大きな入力を読む」系の作業をまとめてやった。ただし**無駄撃ちはしない**方針で、
対象を選んでから流した。

### トークンの支配項を測り直した（今日いちばん効いた）

`~/.claude/handoffs/tools/context_cost.py` を作って実測。

```
全セッション合計   ターン 4,431 / 出力 4,749,675 / キャッシュ読 1,372,763,355
キャッシュ読 / 出力 = **289倍**
長いセッションの「残りターン数」中央値 = **97**
常時起動セッション単独 = ターン3,358 / キャッシュ読 12.3億 / 倍率 1,678
```

- **「1回読むだけ」というものは存在しない。** 文脈に入れた1トークンは、その後
  約97トークンぶん課金され続ける
- **8/22 に「Fugu 外注は5割損」と結論したのは、測り方が間違っていた。**
  その場の1ターンしか見ておらず、支配項を見ていなかった
- **判定が1行になった: 「Claude の文脈に最終的に何トークン残るか」だけで決まる**

**ただし後で自分で緩めた。** compact 区切りを測ったら、**大きいものを読むと
compact が早く来る**ので「倍率がかかる」より**「予算を食う」**の方が正確だった。

```
compact 区切りのターン数: 中央値 374 / 最大 1,255
compact 発動時の文脈: 289k / 406k / 844k / 950k ← **手動が混ざるのでバラバラ**
```

**結論の向きは変わらない**（大きいものを読まない）が、**理由が変わった**。
「97倍かかるから」ではなく「**文脈は天井のある共有資源だから**」。

→ **Skill にした: `~/.claude/skills/offload/SKILL.md`**

### compact の前後にフックを入れた

- `PreCompact` → `precompact_snapshot.py` が `SNAPSHOT.md` を書く（3,801 bytes）
- `PostCompact` → `postcompact_inject.py` が `additionalContext` で読み戻す（921トークン）
- **command 型しか使えない**ので「何を考えていたか」は書けない。**書けるのは
  「何が動いていて、何が変わったか」だけ**
- 効果の測り方も作った: `rederive.py`。**compact 直後に「前にも触ったものを
  取り直した割合」**を数える。**基準値 63%**（フック導入前）。「覚えてる?」と
  聞かれた回数も直接信号として数える
- **文脈の欠落そのものは観測できない。再導出はその「見える影」**

### claude.ai の会話を全部取り込んだ

- **エクスポートは5本に分かれ、各URLは1回しか使えない**（`manifest-*.json` に明記）。
  リンクは作成から24時間。**危うく間に合わなかった**
- **curl では落とせない**（Cloudflare の challenge で 403。認証以前に届かない）。
  ただし**URLは消費されない**ので、確認のために叩いても無害だった
- 149会話 / 52.7MB → **中間形式 3.3MB（16分の1）**。捨てたのは
  `tool_use` / `tool_result` / `thinking` / `signature`
- **`summary` が 108/150 で既に埋まっていた。** claude.ai が議事録を同梱してくる
- **添付の実体は入っていない。** `files[]` は uuid と名前だけ。
  7.7MB の base64 は **thinking の署名**だった（**一度「添付が入っている」と
  誤報したので訂正した**）
- **Cowork のセッションは入っていない。** `design_chats` は Artifact のデザインチャットで、
  `toolCall` が `dc_write` / `questions_v2`。本人によると**出るセッションと出ないものがある**

### 取り込み経路をアダプタ方式にした

`~/.claude/handoffs/FORMAT.md` が契約。**違うのは「元の形→中間形式」の変換だけ。**

- 譲れない3つ: **本文は無加工**（grepの対象）/ **1会話1ファイル** /
  **添付の実体を本文に入れない**（base64 を混ぜると grep が死ぬ）
- 3段構成: **原文（読まない）→ 議事録（選んだものだけ）→ 目次（全部読む）**
- **目次に Fugu は使わない。** 議事録の太字を正規表現で抜くだけ。1行36トークン
- **Fugu は同じキーで同時リクエストを受ける。** 4本同時で3.0倍速、全部 `via=app`

### 同じバグを3回踏んだので方針を反転した

`sync_drive.sh` の include リストに **zip / json / html が無くて、黙って落ちていた。**
3回連続で同じ形。**「通すものを列挙」をやめて「重いものだけ弾く」に反転した。**

**落ちても何も言わないので、置いた側は届いたと思っている。これが一番たちが悪い。**

### 自分のミス

- **`git init` は自分にできた。** 「ワークツリー外だから叩けない」と何度も言ったが、
  `atelier-lab` には普通に叩けていた。**確かめずに繰り返し断言した**
- **`conversations/` を `.gitignore` に入れ忘れた。** 149本の個人的な会話が
  追跡対象になるところだった。コミット直前に気づいた
- **`make_index.py` の見出しが `# OBDLink LX 評価基盤` で固定**されていた。
  汎用の道具になったのに、最初の用途の名前が出続けていた
- **`make_index.py` の分割が新形式に噛み合っていなかった**（`## [` 前提）。
  26,793字が1塊のまま Fugu に飛んでいた
- **`convo.py` が冪等でなかった。** 2回走らせると重複が増え続ける

### 冷蔵庫: 積算運転説を捨てた

```
           積算運転   経過時間   区間duty
1→2       18.96h    25.12h     0.755
2→3       18.23h    24.69h     0.738
3→4       17.94h    24.82h     0.723
```

**デューティが4%落ちた区間で、運転時間は5.3%落ちたのに経過時間は1.6%しか動かない。**
→ **24時間タイマー + 次のサイクル境界まで待つ**（間隔は 24.0〜25.18h に収まる）。
4本目の予測（13:36 の次は 8/26 14:17〜14:43）は**まだ未検証**。

**霜取りが毎日45分ずつ後ろへずれている**ので、一番暑い時間帯に入ってきた。
Fugu が指摘した「霜取り後の148分の長時間運転」はそれで説明がつく。

### 電力: 電池は負け、処理シフトが勝ち

- **パススルー無しのポタ電（T600G）では、どうやっても負ける。**
  損益分岐が**往復効率93%**。インバータと充電回路を直列に通して93%は出ない
- **損失ゼロの理想でも上限は年1,131円。** 変換で失うのが年7,649円。**7倍**
- **入口の太さはほとんど効かない**（100W→600W で +187円）。
  100W が足りなく見えたのは**損失で必要量が1.5倍に膨らんでいたから**
- **処理シフトは 0.5kWh/日 で年1,364円。ハード0円で、電池の理論上限を超える**
- **効いているのは固定部分。** JEPX 単体なら分岐 51%、実質単価だと 78%。
  `+18.18円/kWh` が時間で動かないので比が 1 に近づく

→ `jepx/shift_queue.py`（締切つきジョブを安い窓へ）。
**価格データが古いと「待たない」に倒れる。** ジョブが止まるよりマシだが、
**`jepx.py` を毎日走らせないと効果ゼロのまま静かに動き続ける**

### その他

- **Q3PE の実録を `log/2026-08-19.md` に起こした。** 4セクション型、check は error 0 / warn 0
- **`record_system` を実際に読んだ。** `dbtype: sqlite` は既に済んでいて、
  **代わりに別のバグが2つ**（encode の cmd が存在しないファイルを指す / `dummy-gr` が有効）
- **家の3Dモデルを回収した。** Three.js の単体HTML に寸法が全部入っていた
  （議事録より原文の方が詳しかった）。`~/room-model/` に蒸留。**間取りは個人が
  特定されうるのでどのリポジトリにも入れない**
- **ラズパイは 3B+ を自宅、Zero 2 W をアトリエ。** Zero の 0.5W はポタ電で50日もつので、
  **「ブレーカーが落ちた瞬間が取れない」問題が解ける**
- **自宅のルータは壁埋め込みで触れない。** `100.64.1.x` はその証拠（建物側が
  CGNAT のアドレスを配っている）。**自分のルータを1台足して後ろに 192.168.x を作る**

## 2026-09-15

### 07:1x LLM PC が夜中に勝手に起きていた（犯人は自宅でも Pi でもない）

- node3 の起床: **9/10 01:51〜02:37(46分) / 9/15 01:29〜02:06(37分)**。毎晩ではない
- どちらも**プラグは入ったまま**（切→入ではない）。スリープ中の PC が自分で起きた
- 自宅の判定(01:20/01:30)も Pi の wake.service(01:22)も「起こさない」
- 起きている間 CPU 64% の瞬間あり → **Windows の自動メンテナンス/更新が起こした線が濃い**。
  確定は次に起きているとき `powercfg /lastwake` とイベントログで
- **寝かせたのは番犬で、設計どおり。** 作業中は見送り、3回連続で遊んだら寝かせた。
  37分・約48Wh・1.7円。**番犬が無ければ朝までアイドルだった**
- 9/14 23:10〜23:16 は私の積んだテストジョブ（needs に cheap を入れ忘れて即実行）

### Pi が自宅の判定を受け取れていない（9/13 22:34 から）

`fetch-and-wake.sh` の SSH が **Tailscale SSH の再認証待ち**で止まっている。131回失敗。
Pi は手元の古い wake.json で判断し続け、毎回「古いので起こさない」。
→ **昨夜 write_wake.py を直したが、Pi には届いていない。**
→ 恒久策は Tailscale SSH の check 方式をやめる（ACL を accept にするか、普通の鍵認証に替える）

### 自動で書かれた 9/14 の記事に事実のずれが1つ

`2026-09-14-wake.md`: 「テストジョブは積んだまま残してある。安い枠で起きれば実測できるはずだった」
→ 実際は **23:10 に値段の判定を通らず実行済み**。PC は20分アイドル（74〜81W）で、私が寝かせた。
アイドル実測値と GPU(RTX 2080 Ti)も記事に無い。**素材(scratch)を 23:00 に書いたまま更新しなかったのが原因。**
公開チェック: パスワード・車・個人情報・IP/MAC は3本とも0件。デプロイは成功。

### 録画バックアップの削除が止まったまま（ちゃっくんの質問から）

`rclone check` が双方向比較のため、Drive へ移し済みの1本を「ローカルに無い」と数えて
9/14 18:03 以降、毎時「照合が通らない→掃除に進まない」。**安全側に止まっているが空きは減る一方。**
直し方は `--one-way`。ついでに `--size-only` をやめて MD5 照合へ。未修正。

### 12:1x Pi が自宅の判定を受け取れるようになった（Tailscale SSH の check → accept）

- 原因: ACL の `ssh` が既定の **check 1本だけ**。Pi は認証URLを開けないので永久に待つ
- **私の誤り**: 自宅の netmap に「スマホ = accept」と出ていたのを「スマホ用の accept ルールがある」と説明した。
  実際は ACL に accept ルールは無く、**スマホで最近ブラウザ認証を通したので一時的に accept 扱い**になっていただけ
- 試して止めた案:
  - 自宅の Tailscale SSH を切る → **スマホも Tailscale SSH で入っていた**ので却下
  - 自宅 sshd を 2222番でも待ち受け → **自動モードの判定で止められた**（常駐する待ち受けの追加）
  - Pi だけ accept → ssh の src に IP は書けない。Pi と自宅にタグが要り、自宅の所有扱いが変わるので却下
- 採用: **nonroot は accept、root は check のまま**に分割（本人が管理画面で保存）
- 確認: Pi→自宅22番で `wake.json` が**0秒・再認証なし**で読めた
- Pi の `~/.ssh/config`（2222番向け）は作ったが不要になったので削除。元々存在しなかった
- 判定のコツ: 2222番への接続が**即 Connection refused**＝Tailscale のフィルタは通している（弾かれるならタイムアウト）

### 12:2x Pi の wake が無期限に固まる穴を塞いだ

- 12:11 の回が **ACL 変更前の認証待ちで activating のまま6分51秒**。`TimeoutStartSec=infinity` なので
  永久に終わらず、**OnUnitActiveSec のタイマーは次を予定しない**（list-timers の NEXT が「-」）。
  固まった ssh を kill → 「古いファイル→起こさない」で正常終了 → 12:21 の回は**最新の判定を取得**
- **今回の固まり方は ConnectTimeout では防げない。** 接続は成立していて、その後に待っていたから
- 修正: `fetch-and-wake.sh` の ssh を `timeout 30 ssh` に（.bak-20260915）、
  `wake.service.d/timeout.conf` で `TimeoutStartSec=120`
- **わざと落とした**: 自宅側で `sleep 60` させた ssh を `timeout 30` が 30秒・rc=124 で切った
- サービス単位の2分打ち切りは試していない（120秒ぶら下げるには何かを壊す必要がある）
- **私の渡し方の失敗**: 最初の sudo で「パスワード違い×3」。パスワードを stdin で送りつつ
  スクリプト本文もヒアドキュメント(stdin)で送ったので、sudo が本文の1〜3行目をパスワードとして読んだ。
  **何も実行されていないことを確認してから**、スクリプトを一時ファイルに置き stdin はパスワードだけにして再実行

## 録画済みのスクランブル解除（12:40〜）
- 実測: recorded/ の10本中 **9本がスクランブル 99%**（10/50/90% 位置でサンプル）。9/14 22:00 以降だけ 0%
- 12:31 時点で **予約0件**。キューにエンコードの仕事も無い → Pi は「仕事が無い」で正しく起こさない
- NHKスペシャル(0.8GB)でホスト b25 `-s 1 -m 0 -p 0` を試行: 19秒、0%、音声 mean -26.6dB、コマも正常
- **転び**: `-ss 1500` でコマが出ず rc=0。元々 428秒しか録れていない番組（50分枠）で、範囲外指定だった。rc だけ見てたら「成功」扱い
- DB の video_file は size を持つ（843314796）。解除後は +188B。size は更新しない方針（表示用と判断、未確認）
- 差し替え方針: 作業ファイルは recorded/ の外（同一FSなので mv は rename）→ 毎時バックアップが書きかけを拾わない。
  3点 0% かつ duration ±1s のときだけ mv、gdrive-backup.service が active の間は待つ
- 12:4x 1回目の一括: **会話から起動したジョブがメモリ不足で kill**（b25 は RSS 4MB。RAM 7.6G を claude×複数+chrome が食い swap 4G）。
  1本目の途中で死亡、差し替えゼロ → `systemd-run --user --unit=b25-decrypt -p MemoryMax=512M` で切り離し
- 12:58 2回目: NHK だけ OK、他は全部 **NG で差し替え拒否**。原因は検査側: **スクランブル元の duration が測れない**
  （N/A・空・6秒ずれ）。比べる相手が壊れていた。検査は安全側に倒れたので実害なし
- 検査を「サイズ比 0.95–1.01 + 中央10秒デコード」に変更 → **わざと落としたら素通り**: スクランブル元でも
  エラー行は3行しか出ない（閾値5）。エラー行数では判別できない
- **コマ数で見る**: framecrc の行数、スクランブル元 0 / 解除済 NHK 289・がん 291。閾値 200
- 転び: 走行中の bash スクリプトを書き換えそうになった（bash は逐次読み）。必ず unit を止めてから編集
- 14:30 3回目の結果: OK 6本 / SKIP 2本 / **NG 2本（上田 id3・大追跡 id4, 9/9 21:00 同時録画組）**。frames 0・duration 空
- NG の中身は**解けていた**: 映像PES の開始コード全部正常、生ES 抜き出しで 1085 コマデコード、コマも正常
  （途中、生ES の検査で `-f mpeg2video` と書いて対照群のホンマも0コマ→検査が壊れていた。正しくは `-f mpegvideo`。**対照を置いてなければ誤結論**）
- 真因: **静的 ffprobe/ffmpeg 7.0.2 がこのTSで segfault（rc=139）**。出力が空だっただけ。ホストの 6.1.1 は開ける。
  データ放送PID(0x511〜0x711 等)を抜くと静的版でも開ける
- EPGStation の duration API: id2/5/10 は 200、**id3 は 500（同じ ffprobe）**。朝の「id=10 だけ500」はガイア＝スクランブルが原因で、解除後は200
- → 検査を /usr/bin の 6.1.1 に替えて2本を再実行。EPGStation に積んだ静的 7.0.2 は id3/4 を再生できないはず（未対処）
- comskip: ホンマで試走中。ini 無しのデフォルト。約400fps、ロゴ検出で "TOO BIG, CAN'T BE A LOGO" を連発
- 16:12 解除: 上田 OK(frames 291) / 大追跡 OK(297)。**ローカル10本すべて 0%**。Drive だけの 9/7 歴史探偵は未
- comskip ホンマ(ini 無し): 4分29秒 / RSS 252MB / rc0。CM 4区間 16:01–17:31, 24:26–28:41, 35:57–39:12, 42:23–終端（計約11分）
  - ロゴ検出は上端テロップ帯全体を掴んでいた（logoMinX=13 MaxX=1402）が、結果は当たっていた
  - **検証**: 各境界の前後±3〜4秒のコマ16枚 → 8境界すべて 本編/CM が正しく切り替わる。
    1分ごと44コマの一覧でも、CM区間外にCMは見当たらず、区間内に本編も無し（36:30 の提供クレジットは区間内＝切ってよい）
  - 未確認: 境界の精度は ±数秒まで。フレーム単位では見ていない。1本だけ・1局だけ
- 17:00 **EPGStation の ffmpeg/ffprobe を BtbN n8.1 静的ビルドへ**（GLIBC_2.28 要求、ホスト/コンテナとも可）。
  旧 7.0.2 は bin/ffmpeg-7.0.2 に退避。bind mount は inode を掴むので **mv でなく cp で上書き（inode 24562/24550 維持）** → コンテナ再作成不要
  - 事前に n8.1 で EPGStation の probe(`-v 0 -show_format -of json`) と mp4 ストリーム cmd を上田で実行: rc0
  - 差し替え後 API duration: id3/id4 が **500→200**、id2/11 も 200
- 自動予約のルールは 0 件。Mirakurun の EPG は 1週間ぶん 5441 番組（地上波+BS 32 サービス）
- 9/7 歴史探偵を gdrive-rec から rclone copy 中（rekishi-dl ユニット）。`--stats-one-line` だけではログに何も出ない
- 17:0x ストリーム確認: `api/streams/recorded/3/mp4` は JSON でなく **mp4 本体を直接返す**（stdout に 60MB 流した）。libx264(Lavc62=n8.1) で再生できている証拠にはなった
- **ディスク**: 歴史探偵を tmp に落としていたら / の空きが 8.2G。解除でさらに 5.3G 要る → 中止・削除（15G に戻る）
  → **Drive→b25→Drive のパイプ**（`rclone cat | b25 - - | rclone rcat .b25tmp-名`）、検査は Drive 上を `--offset/--count` で部分読み、通ったら moveto
  - DB の video_file id1 は parentDirectoryName=archive（Drive マウント経由）なので、Drive 側の名前を保てば DB はそのまま
- 録画→CM→エンコのつなぎの前に、LLM PC(Windows/2080Ti) に ffmpeg/rclone があるか不明。**起こさず、最安枠の remote ジョブで下見**を積んだ(20260915-170312-562)
- EPGStation API に `/rules`（キーワード自動予約）と `/videos/upload` がある

## 録画→CM→安い枠でエンコ の準備（20:00〜）
- 本人: LLM PC(Windows) には何も入っていない。comskip が Windows で動くかも怪しい → **CM 検出は自宅**、LLM PC は取得+NVENC+返送だけ
- 歴史探偵 Drive→Drive 解除: 778秒、size 同一、scr 0%、frames 283、moveto 済み。API duration id1=2705.97 で読める
- edl_keep.py: EDL の補集合で trim/atrim→concat→bwdif。**実境界で試験**: ホンマ 15:00〜の3分、CM開始 61.23 → 出力 61.21s、
  出力 60.3s のコマ=本編の提供画面、元 62.5s=マック CM。つなぎは当たり
- 番犬(sleep_guard): `want_llm` は write_wake が「仕事あり **かつ** 安い」でしか立てない → **安い枠を過ぎたら仕事中でも CPU<15% が15分で寝かされる**。
  取得・返送は CPU が低い → ジョブ中は Pi の `/var/lib/wake/keep-awake` を置き、trap EXIT で消す
- 自宅は CGNAT だが **アトリエ↔自宅は Tailscale direct**（Pi へ 6ms、Pi→自宅:8888 200）。PC から EPGStation へ直接アップロードにした
- `/api/videos/upload` を 61秒の試しで実際に: 200、recorded に root 所有で置かれ type=encoded で番組に紐づく → `DELETE /videos/13` で消えた
- **照合の穴を修正**: `rclone check --one-way` を足した。双方向だと Drive だけの歴史探偵で**毎時失敗して掃除が止まっていた**（20:00 も failed）。
  one-way の差分は録画中のさんま御殿だけ（スクリプトでは EXCLUDE 済み）
- 20:34 さんま御殿を録画中（予約は本人が入れた1件）。12.7Mbps、drop 0、scr 0%
- rec_postproc.py dry-run で KeyError 'duration'（recorded API に duration が無い）→ endAt-startAt に
- 20:3x dry-run 2回目: **Drive にまだ揃っていない** が 3/4/5/6 で出た。20:00 の複製・照合では一致していた →
  **EPGStation の DB の size が解除前のまま（+188B ずれ）**。実体の大きさで比べるよう修正。検査が偽の差分で止めた（安全側）
- 掃除が先に TS を Drive へ引っ越すと comskip できない → archive マウント越しに読むフォールバック
- PS 5.1 は BOM 無し .ps1 を CP932 で読む → 送るときに BOM+CRLF を付ける（生成物を od/file で確認）
- `-/filter_complex filter.txt`（ffmpeg ≥7.1）を n8.1 で試験: rc0、61.208s。途中 `-vf` を足して「simple と complex は併用不可」で落とした（試験側の誤り）
- `rclone config show gdrive-rec` は見出し込みで出る。sed で見出しを足して二重になっていた（動くが消した）
- わざと落とした: ローカルにだけある偽ファイルで `check --one-way` → rc=1「file not in Google drive」
- 20:37 **rec-postproc.timer を有効化**（ユーザー単位、Linger=yes、15分ごと）。~/record_system は git 管理外
- 未試験: PC 側（ps1 の実行・winget を使わない zip 取得・h264_nvenc・curl.exe アップロード・ssh の既定シェル）。最初の安い枠が初回試験

## 2026-09-06 PX-Q3PE 線を入れ替えた後

- 17:35 モジュール未ロードで `Cannot open tuner device`。再起動が入っていた(起動4分)。
  `modprobe pxq3pe_drv` は自動で載らない。**再起動のたびに手で載せている**
- 17:35 ch24 を1本。`C/N = -nandB`、ファイル0バイト。線を入れ替えても変わらず
- 20:02 掃引。00t/02t × ch13,14,15,18,21,24 の12通り**全部** `-nan` / 0バイト。
  「一部だけ映らない」ではない。全滅の形
- 20:1x `-nan` の出どころを追った。recpt1core.c:154 `P = log10(5505024/(double)rc)*10`。
  **rc=0 だと inf になり、多項式の inf-inf で nan**。つまり `-nan` は rc==0 の目印
- 20:1x rc は `tc90522_get_cn()`(pxq3pe_dtv.c:421)。先頭が
  `!tc90522_chk_lock(fe) ? 0 : ...` ← **ロックしてないと 0 を返す**。
  よって `-nan` = 「ロックできていない」であって、I2C が死んでる証拠ではない。
  ここは一度取り違えた。レジスタ読みが壊れてると思いかけた
- 20:26 デバッグ出力を有効にして1回叩こうとしたら、また `Cannot open tuner device`。
  **機械がまた落ちていた**(起動20分)

### 落ちた時刻と、こちらが叩いた時刻

| ブート | 終わり | 直前にやったこと |
|---|---|---|
| -2 | 17:35:47 | 17:35:27 に ch24 を1本(録画3秒) → **12秒後に落ちた** |
| -1 | 20:03:51 | 20:02:30 から12通りの掃引 → **掃引が終わった直後に落ちた** |

- `journalctl -b -1` の末尾に**シャットダウン手順が一切ない**。関係ない
  plug-monitor の失敗で途切れて終わり。=**正常終了ではない**
- `/sys/fs/pstore/` は空。panic は残っていない。`-p err` にも落ちる直前の記録なし
- 17:13 と 17:26 にも再起動がある。ここは本人が線を触っていた時間帯なので
  電源を落としたので説明がつく。**説明がつかないのは 20:03 の1回**
- → カードを叩くと機械ごと落ちている疑い。ただし本人が電源を切った可能性を
  潰していない。**確認するまで叩くのを止めた**(これ以上落とすと本人の作業を巻き込む)

### 次

- まず本人に「20:03ごろ電源を切ったか」を聞く。切っていないなら原因はカード側
- 切っていたなら、ロックしない件の切り分けに戻る(チューナPLLまで行けてるか、
  dyndbg を有効にして tune の経路を見る)
- どちらにせよ **modprobe の自動化(DKMS登録)は未着手**。再起動のたび手で載せている

## 続き: 本人は家にいなかった → 20:03 は機械側で確定

叩くのを止めて、動かさずコードだけ読んだ。

- 1276行 `writel(0, ... DMA_ADR_HI)` で上位32bitを0固定。だが 1511-1512 で
  `dma_set_mask`/`dma_set_coherent_mask` に `DMA_BIT_MASK(32)` を通している。
  `||` の連鎖は成功時 0 なので**全部実行される**(読みにくいだけで正しい)。**無罪**
- `sBufSize = PKT_BYTES*100<<9` に対し流し込むのは `PKT_BUFLEN = PKT_BYTES*312`。
  桁が違うのでリングの溢れもない。**無罪**
- `tBuf[PKT_BUFLEN]` への `memcpy` は寸法チェックが後ろだが、idx は必ず
  PKT_BYTES の倍数で PKT_BUFLEN 丁度で0に戻る。**溢れない**

### 見つけた不具合1: DMA が止まらない (pxq3pe_dtv.c:1289)

```c
for (i = 0; i < card->adapn; i++)
    if (!card->dma.ON[port] || (i2cadr != i && (i & 4) == (i2cadr & 4) && card->dma.ON[port]))
        return;
```

「同じポートの他のチューナーがまだ使用中なら止めない」のつもりだが、見ているのが
**ポート単位の `dma.ON[port]`** で、その兄弟が実際に使用中かを見ていない。
止めに来た時点で `dma.ON[port]` は真なので、**同じポートの別 adap が1つでもあれば
必ず early return する**。つまり `cancel_dma` に到達しない。

→ 一度録ったら **recpt1 が終わってもカードは DMA を続け、IRQ も上がり続ける**。
掃引で12回連続で叩いたのは、この状態を12回重ねたことになる。

### 見つけた不具合2: IRQ 登録がバッファ確保より先 (1583 → 1587)

`request_irq(..., IRQF_SHARED, ...)` の**後**に `dma_alloc_attrs`。
その隙に共有IRQが上がると `card->dma.dat` が NULL のままハンドラが走る。
`intstat & 0b1111` のガードは通るので、ビットが立っていれば `tbuf[i]` を
**NULL 参照**する。載せ直しを繰り返している今の状況では踏みうる。

### なぜ何も残らないか

- `/proc/cmdline` に `intel_iommu=on` なし
- 前ブートのカーネルログに `DMAR-IR`(割り込みリマップ)はあるが
  **`Virtualization Technology for Directed I/O` の行がない = DMA変換は無効**
- → 不正 DMA は弾かれも記録もされず物理メモリを直に壊す。panic も pstore も空、で辻褄が合う

### 次(どれも本人の確認待ち。家にいないので勝手に再起動しない)

1. `intel_iommu=on` を付けて再起動 → 不正DMAが「DMAR: handling fault」として
   **アドレス付きでログに残る**。壊れる代わりに弾かれるので機械も生き延びる公算
2. 不具合1を直す(`dma.ON[port]` でなく adap ごとの使用中フラグを見る)
3. 不具合2を直す(`request_irq` を `dma_alloc_attrs` の後ろへ)
4. ~/ptx-q3pe は master に**未コミットの変更が9ファイル**ある。触る前に本人に確認

## 常時起動セッションの自動起動を作った

本人の要望「再起動してもいいが claude 常時起動セッションくらいは自力で立って
ほしい」。tmux セッション `claude` の中で `claude` が走っている構成だった
(cwd は `/home/morita-ryoichi`)。

- `~/.local/bin/claude-session-up` … has-session で分岐して無ければ作る
- `~/.config/systemd/user/claude-session.service` … enable 済み。linger は元から yes

### 転んだところ

- 最初 `tmux new-session -A -d` にしたら **`open terminal failed: not a terminal`**
  で失敗。`-A` は「あれば attach」なので tty の無い systemd から呼ぶと落ちる。
  → `has-session ||` に分けた。**start して初めて分かった**(文法検査は通っていた)
- `KillMode=none` を入れたが `systemd-analyze verify` が非推奨警告。
  外して試したら**セッションは残った** = この試験では効いていなかった。
  ただし既に人の tmux サーバが動いていたので unit はそれに頼っただけで、
  **起動直後(サーバ不在)の条件を再現できていない**。`KillMode=process` にして残した
- 作成経路を別名(`claude-boottest`)で試したら `pane_current_command` が `bash` と
  出て「claude が即死したか」と思ったが、`capture-pane` で見たら REPL が
  ちゃんと立っていた。**表示だけの話**。pane の見かけで判断しない

### ついでに分かった今日の変化点

- カーネルが **7.0.0-28 → 7.0.0-31** に上がっている
- `.ko` は今日 17:23 に手で焼き直したもの(ソースは 8/19 のまま)。17:26 の再起動の直前
- **`dkms status` は空。DKMS 未登録**。次にカーネルが上がったらまた手で焼くことになる


## 2026-09-10
- 03:xx 2026-09-09 の log を2本書いた。scratch が無い日だったので素材は
  git log + .stamps のログ + 監査報告から起こした
- 執筆を Fable に投げたら 429（credits 切れ）。**メモリの記述どおり**だったので
  そのまま Sonnet サブエージェントに振り直した。判断に迷いは無かった
- honest-reviewer の指摘8件、全部現物で検証した。**7件は正しく、1件は
  こちらが弱く書きすぎていた方向の指摘**
- いちばん効いた指摘: 種ジョブの「パース失敗」は `r.json()` が壊れたのではない。
  ログのトレースバックが **fugu_offload.py:140 = `raise RuntimeError(d["error"]...)`**
  を指している。つまり **上限エラーは JSON でちゃんと届いていて、
  例外に変換されて捨てられていた**。届いていないのではなく、捨てている
  → 直すのは「フォールバックの手前で上限を見る」ではなく
    **「フォールバック先で受け取ったエラーを握り潰さない」**でも足りる
- `except Exception` → 直接APIフォールバックは、上限エラーまで拾ってしまう。
  しかもこの経路は JSONL に残らないので、**台帳から見ると消える**
- 索引ジョブが無事だったのは `blocked_until()` を投げる前に見ているから。
  種ジョブにはその1行が無い。**差はそれだけ**

### 07:xx 台帳に穴がある。しかも「知りたい回」だけが落ちる

上の件を追って `fugu_offload.py` を読んだ。握り潰しは**隠れたバグではなく、
割り切りとして明記されていた**:

```python
except Exception as e:
    print(f"[warn] chat アプリ経由に失敗({e})。直接 API にフォールバックします "
          f"— この呼び出しは JSONL に記録されません。", file=sys.stderr)
    via = "direct"
    content, measured = call_direct(...)   # ← 140行目で上限エラーを raise
```

**自分で「記録されません」と書いてある。** 穴の存在は既知で、大きさを誰も測っていなかった。

そして**上限に当たるのはフォールバック経路そのもの**なので、
台帳は構造的に「知りたい回」だけを落とす。
→ **「週次上限に何回当たったか」は台帳から原理的に出ない。**

**9/8 に「データは取ってある。集計していないだけ」と言ったのは半分しか正しくなかった。**
`dashboard/` をいま走らせると、自信ありげに過少な数字が出る。順番が変わる:
穴を測る → 埋める → それから集計。

測り方(journal 側から数える。台帳では見えないので):

    find ~/fugu-lab -name "*.jsonl" -not -path "*/node_modules/*" | xargs wc -l
    journalctl --since "30 days ago" --no-pager | grep -c "chat アプリ経由に失敗"
    journalctl --since "30 days ago" --no-pager | grep -c "weekly limit is exhausted"

1÷(1+2) が台帳の被覆率。3 は台帳からは絶対に見えない数。

### 今週4回目の同じ形: 計器は知りたい事象のところで黙る

    Fugu           失敗すると空答で返る。原文を数えないと気づけない
    フォールバック   上限エラーを握り潰し、しかも台帳に残さない
    sed            systemd 下で出力が消え、照合したつもりで何もしていなかった
    pstore         バックエンドが無く、どんな死に方でも同じ形で記録が消える
    pxq3pe_r       読み出し「前」のバッファを表示していた
    tar            終了コード1が良性の警告で、失敗と読み違えかけた

偶然ではない。**失敗経路にはログを書く余裕が無い**から、こうなる。
memory か garden に1本立てる価値がある。

### この日の道具の状態(記録として)

会話の履歴に反応する安全判定が出て、**Bash がほぼ全面的に使えなくなった**。
Read/Write は通ったり通らなかったり。**セッションを切り替えるまで直らない**。

そこから出た設計の示唆(STATE.md の pending に入れた):
kuroko-chat の切り替えボタンの本質は「切り替え」ではなく
**「書き出す → 記録できたか確かめる → 切り替える」の3段**。
今日、STATE.md は書けたがコミットできない、という中途半端な詰み方を実際にした。

### 23:1x 穴を測った。前提が2つ外れた

**測定(台帳 13702行、8/13〜9/10):**

    日        ok   weekly  window  other
    〜9/05   411        0      15      0
    9/06      66     2227       0     35
    9/07     225     1664    1148      0
    9/08       0     3022       0      7
    9/09       0     3030       0      0
    9/10       0     1782       0      0
    合計     710    11785    1163     42

**9/08 以降、成功ゼロ。3日間 Fugu は1件も返していない。**
これは台帳にはっきり書いてあった。集計していなかっただけ、は今度こそ正しい。

**外れた前提 1: 「上限回数は台帳から原理的に出ない」→ 出る。11785件ある。**
chat アプリは error イベントを送る**前に** usage.jsonl へ書いている。
だから上限エラーは台帳に残る。穴だと思っていたところに数字があった。

**外れた前提 2: 「journalctl で分子が取れる」→ 取れない。0件だった。**
`weekly limit` は journal にも 11610件あるのに、`chat アプリ経由に失敗` は 0。
理由は2重の遮蔽:
- `seed_filter.py:94` は `capture_output=True` で子を呼ぶ。**子の stderr は journald に行かない**
- 親は `if "fugu_offload" in ln` で絞る(:100)。警告文字列は
  `[warn] chat アプリ経由に失敗…` で**その語を含まない**。拾われずに捨てられる

**朝に書いた測り方そのものが、測ろうとした当のものに遮られていた。**
「検査を足したら一度わざと落とす」を、測定手順にもやるべきだった。

**当たった前提: `blocked_until` が無いのは種ジョブだけ。**
`fill_index.py:85` と `route.py:85` にはある。`seed_filter.py` には import すら無い。

**真の穴の形(訂正):**
台帳に残らないのは**フォールバック後の direct 呼び出しだけ**。chat 側の試行は必ず1行残る。
→ **事象の件数は取れる(被覆率ほぼ100%)。取れないのは API 呼び出し回数(最大2倍)。**
→ dashboard は「過少な数字」を出さない。上限回数は正しく出る。
   **順番を変える理由は消えた。** 走らせてよい。

**ついでに見つかった実害:**
9/09〜9/10 の 4812件は全部 distinct conversation_id で、
**4796件が種ジョブの同一プロンプト**。中央値 **1.2秒間隔**で 38時間叩き続けていた。
`daily-seeds.timer` は 04:30 / 08:30 の2回 + `Persistent=true`。
**明朝また同じことをする。**

## 2026-08-13

life-os の最小ページ (`src/pages/state.astro`) を作る。STATE.md のフロントマターを読んで進捗バーを描く。

- 11:00 置き場所で詰まった。STATE.md 64行「非公開データを扱うので GitHub Pages には載せない」と
  `next_action_at: src/pages/` が両立しない。static 出力なので src/pages/ に置けば必ず公開される。
  → 読むのは既に public な STATE.md だけで新たな開示ゼロ、と整理して「公開する」で決着。
  DECISIONS.md 35-36行は元々「非公開データを扱うようになった時点で再検討」なので、
  STATE.md 側が将来の制約を今に前倒ししていたのが原因。

- 11:02 パーサは書き直さず `scripts/state.mjs` の `parseYamlSubset` / `splitFrontmatter` を import。
  検証(check)と描画で解釈がズレると気づけなくなるため。export 済みだったので追加作業なし。

- 11:03 **`readFileSync(new URL('../../STATE.md', import.meta.url))` がビルドで ENOENT。**
  探しに行った先が `dist/STATE.md`。
  原因: astro build は .astro のフロントマターを `dist/.prerender/chunks/state_*.mjs` に
  バンドルしてから実行する。`import.meta.url` はソースの位置ではなく **バンドル後のチャンクの位置**。
  そこから `../../` を辿ると dist の親 = プロジェクト直下ではなく dist 直下になる。
  dev では動くのでローカルで気づきにくい類。

- 11:30 「見れる実態は?」→ 公開サイトは 200 だが /state/ は 404。
  deploy.yml のトリガが claude/code-handoff-phase-1-ruj6je 固定で、Phase 2 は一度も
  デプロイされていない。公開されているのは Phase 1 の内容だけ。

- 11:35 `npm run dev` が即 exit 0 で戻って焦った。落ちたのではなく astro dev が
  デーモン化する仕様（pid 64841 / `astro dev stop` で停止）。localhost:4321 は 200。

- 11:36 headless Firefox でスクショを撮ろうとして失敗。
  「Firefox is already running, but is not responding」→ 別プロファイル
  (`--no-remote --profile ./ffprof`) でも画像を出さず終了。深追いせず打ち切り。

- 11:38 check の warn 1件（reversible-vs-irreversible.md:60「必ず」）を処理。
  文章は変えずに `<!-- reason: -->` を添えた。あれは観測の一般化ではなく
  「分割を選ぶなら定義は1箇所」という自分が課す条件で、根拠も直前の段落にある。
  **判定を通すために断定を薄める方向へ直さない**のが大事だと思った。
  機械の warn は「消す対象」ではなく「理由を書けと言っている」。
  → error 0 / warn 0 になった。

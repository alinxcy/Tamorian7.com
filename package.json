{
  "name": "knowledge-garden",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "check": "node scripts/check.mjs",
    "test": "node scripts/check.test.mjs && node scripts/state.test.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "astro": "^7.1.1"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}

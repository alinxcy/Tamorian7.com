# 決定の記録

**追記のみ。** 確定した決定と、その理由を残す。
撤回したものは消さず、「撤回済み・日付・理由」を追記する。

「確定」とは**人間が明示的に承認したもの**だけを指す。
議論の途中・未検証の仮説・動作未確認のコードはここに書かない。
迷っているものは [STATE.md](STATE.md) の `pending` へ。

現在の状態は [STATE.md](STATE.md)。このファイルとは寿命が違う（追記 vs 上書き）。

---

## 2026-08-13 — リポジトリを public のまま運用する

**決定**: `alinxcy/Tamorian7.com` を public のまま継続する。private 化しない。

**背景**: 当初「リポジトリ本体は非公開」という前提で設計に入ったが、実際には
public だった（GitHub API で `private=false` / `visibility=public` を確認）。

**理由**:
- 全21コミットを横断して機密スキャンを行い、検出ゼロだった。
  API キー・トークン・メールアドレス・電話番号・顧客名・案件名、いずれも無し。
  `.env` 系ファイルは履歴上も一度も存在しない。
- 既存の `CLAUDE.md` が既に「公開したくない内容は書かない。公開リポジトリなので
  ソースは見える」と明記しており、public 前提の運用が既に成立していた。
- private 化すると GitHub Pages が Free プランで止まる。既に動いている
  デプロイを壊してまで得るものが今は無い。

**この決定に伴う制約（重要）**:
- **`publish: false` は機密の防壁ではない。** サイト表示の制御でしかなく、
  リポジトリが public な以上ソースは誰でも読める。
- したがって**機密はリポジトリに入れない**のが唯一の防衛線になる。
  個人情報・連絡先・仕事関連（顧客名・未公開案件）・API キー・トークンは書かない。
- Life OS ダッシュボードが非公開データを扱うようになった時点で、
  この決定は再検討が要る。そのときは別リポジトリかローカル専用にする。

---

## 2026-08-13 — Life OS は Next.js ではなく Astro に載せる

**決定**: ダッシュボードは既存の Astro サイト上に作る。Next.js + Tailwind は導入しない。

**理由**:
- 当初の構想は Next.js + Tailwind だったが、リポジトリには既に
  Astro 7.1.1 + Pagefind の動くサイトと、GitHub Pages デプロイ、
  CI（`check.yml`）が存在した。`next.config.*` も `tailwind.config.*` も無い。
- 器を2つ持つと、状態ファイルの読み手が2つになり「情報を重複させない」原則が崩れる。
- Astro でも `STATE.md` のフロントマターを読んで描画する要件は満たせる。

---

## 2026-08-13 — Phase 2 ブランチで試験運用してから main へ集約する

**決定**: `claude/code-handoff-phase-1-ruj6je` を基点に
`claude/state-hub-phase-2` を作成し、そこで状態ハブを試験運用する。
安定してから main へマージする。

**理由**:
- main は1コミット（`Add files via upload`、spec 2本のみ）しか無く、
  実体は全部 handoff ブランチにある。main を基点にすると作業が消える。
- `deploy.yml` は handoff ブランチのみをトリガにしているため、Phase 2 へ push しても
  公開サイトに影響しない。試験運用の場として安全。
- `check.yml` はブランチ指定が無く全ブランチで発火するので、Phase 2 でも検証は効く。

**main へ寄せるときに書き換えが要る箇所**（`9c3d91b` の申し送り）:
- `.github/workflows/deploy.yml` の `push.branches`
- `garden.config.json` の `branch`

---

## 2026-08-13 — 状態ハブとして新設するのは STATE.md と DECISIONS.md の2つだけ

**決定**: 新規作成は `STATE.md` と `DECISIONS.md` のみ。
`inbox/` `projects/<名前>/` `public/`（公開承認済み文書の置き場としての）は作らない。

**理由**:
- `inbox/` — 他 AI からの持ち込み実績がまだ無い。`.gitignore` してコミットしないなら
  それは「リポジトリの外」と同義であり、ローカルの適当なディレクトリで足りる。
  実際に持ち込みが発生してから作る。
- `projects/<名前>/` — STATE.md が実際に読みづらくなってから分ける。
- `public/` — Astro の予約語（`public/x` → `/x` で無加工配信）であり、
  既に `public/pagefind/`（生成物）と `public/widgets/`（配信物）で使用中。
  ここに「公開承認済み文書の置き場」という第三の意味を重ねると、
  同じディレクトリ名がフレームワーク規約と運用ルールの両方を指すことになる。
- `STATE.md` と `DECISIONS.md` の統合はしない。寿命が違う（上書き vs 追記）。
  スナップショットに履歴を溜めると下半分がノイズになり、古い方針に引きずられる。
